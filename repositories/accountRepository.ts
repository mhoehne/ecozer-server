import { AccountModel } from '../models/accountModel';

type QueryType = { [key: string]: string | number | boolean | RegExp };

type SortOrder = 'ascending' | 'descending';

// I STOP HERE, TOO MANY ERRORS
